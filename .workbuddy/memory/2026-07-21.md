# 2026-07-21 工作日志

## 项目对接与架构梳理
- 通读 XJTU-ODPlatform 全仓,理解三层 Monorepo 架构(platform 引擎 + web-backend Django + web-frontend Vue)。
- 关键文件已读:paths.py / orchestrator.py / registry_utils.py / train_model.py / model_train/service.py / settings.py / predictor.py / inference/services.py / types/index.ts / train.yaml / VOC2028.yaml / 两个 README / pyproject.toml ×2 / .gitignore / constants.py。
- 绘制两张 SVG 可视化:① 三层架构总览(三端模块 + 共享资产 + 依赖箭头);② 数据到推理主链路(原始数据→管线→训练→推理→输出,带 CLI 命令与产物路径标注)。
- 沉淀长期备忘到 `.workbuddy/memory/MEMORY.md`(架构、模块职责、契约、运行入口)。

## 观察到的现状(供后续参考)
- 引擎通用,Web 主题为脑部 MRI 肿瘤检测;但 data/raw 实际是 VOC2028 安全帽数据集(hat/person),models/trained 尚未建立,Web 端默认 demo 合成检测。
- git 历史显示作者是「雨霓同学 / Matri / Xiao Liang」,学生实训项目,有 `【学生成果物】第X组_...` 交付目录。

## 数据师任务:重置/转换/划分/校验增强(任务1-7)
用户担任数据师,数据集 `data/raw/VOC2028`(Pascal VOC,7581 图,hat/person)已就位。对三块做能力增强:

- **任务1 重置备份 ✅**: `cli/reset_project.py` 加 `--backup`/`--backup-dir`,重置前原样复制待删目录到 `backups/reset_<ts>/`,写 `backup_manifest.json`,备份失败中止重置。`.gitignore` 加 `backups/`。
- **任务2 转换扩展 ✅**: 新增 4 转换器 `converters/{labelme,dota,cvat,createml}.py`。`constants.AnnotationFormat` 注册 4 格式。`_geometry.py` 加纯 Python `read_image_size`(JPEG/PNG/BMP 头,无依赖,供 DOTA/CreateML 读尺寸归一化)。registry 验证 7 转换器全注册。
- **任务3 划分扩展 ✅**: 补全幽灵策略 `stratified_multilabel`(Sechidis 简化)+ 新增 `group_split`(同组不拆散)+ `stratified_weighted`(主类分层)。`SplitManifest` 统一到 `common/lineage`(v0.4),删除重复的 `split/manifest.py`(orchestrator/materializer/yaml_writer 三处 import 已改)。orchestrator 加 `group_by_prefix`/`groups_file` 透传,修返回值拼写 `contract_figure→contract_fingerprint`。CLI `transform_data` 加 `--group-by-prefix`/`--groups-file`。
- **任务4 划分报告 ✅**: 新增 `split/report.py`,`build_split_report` 产出类别分布/bbox 尺寸分桶(COCO)/集间 JS 一致性/指纹,落 `split_report.json`+`split_report.md`。orchestrator 挂载,失败不阻断主流程。
- **任务5 校验扩展 ✅**: 新增 `checks/{duplicate_image,annotation_integrity}.py`。duplicate=sha256 精确+aHash 分桶近似(PIL 不可用降级);integrity=越界/零面积/倒置/解析错。registry 验证 7 check 全注册。
- **任务6 分布分析+报告增强 ✅**: 新增 `data_validation/distribution.py`(非 check 纯统计:类别分布/尺寸/集间漂移 JS)。`report.py` 的 `ValidationReport` 加 `distribution` 字段,`to_dict` 加章节,新增 `write_quality_report_md`。`service.py` 串起 analyze_distribution + 落 `quality_report.md`。
- **任务7 集成验证 ✅**: orchestrator 加 `_resolve_raw_dirs` 智能识别 raw 布局(images/annotations 或 JPEGImages/Annotations)。VOC2028 全链路跑通:transform(pascal_voc→YOLO+划分+split_report) → validate(7 check+分布报告+quality_report.md)。指纹 b1e5a25 一致,120561 框全合法,分布一致性 max JS=0.0008。
  - duplicate_image v1.1: aHash→dHash(结构敏感),近似重复降为 INFO(曾因 aHash 误报 37960 拉总评 ERROR),阈值收紧到 4。修复后近似 14278(INFO),完全重复 37(ERROR,真数据泄漏)。
  - odp-gen-config 报错诊断:odplat 环境安装的 entry point 是旧版 `generator:main`(generator.py 无 main),当前 pyproject 已改 `cli.gen_config:main`。修复=`pip install -e apps/platform` 重装,或临时 `python -m od_platform.cli.gen_config train`。

## 追加:图片完整性 check + 问题图片清理(任务8)
用户问"训练时 ultralytics 报 corrupt JPEG 为何质检没查出"——之前只有标注完整性+重复图 check,缺图片文件完整性检测。
- 新增 `checks/image_integrity.py`(ImageIntegrityCheck):PIL verify()+load() 两关检测损坏/截断/无法解码,ERROR 级,details.problem_images 存清单。registry 现 8 check。
- `report.py` 的 `write_quality_report_md` 加"⚠️ 问题图片清单"章节(列 split/图片/问题),附提醒"请运行 odp-validate --purge 输入 YES 确认删除"。
- `service.py` validate 结束若有问题图片,logger.warning 提醒。
- `cli/validate_data.py` 加 `--purge`/`--yes`:`_purge_bad_images` 交互式问大写 YES,确认后删图+同名标签(images 段替换 labels),提示重跑 transform 重建 manifest。
- 实测 VOC2028:ImageIntegrityCheck PASS(7581 图完整)——因 ultralytics 训练时已自动修复 corrupt JPEG 并覆盖,故查不到;但 check 对未来/raw 数据有效。
- **purge 修复**:`_purge_bad_images` 原本只清 ImageIntegrityCheck 的损坏图(查不到就"没有问题图片")。改成 `_collect_purge_targets` 同时收损坏图 + DuplicateImageCheck.exact_duplicates(全量,删重复保原始)。duplicate_image.py 的 details 加 `exact_duplicates`(全量)+ 每条加 `path`。service.py 提醒也合并两类。重跑 `odp-validate --purge` 现在会列 37 张完全重复图 + 问 YES。

关键约定:python 环境用 `D:/Anaconda3/python.exe`(有 yaml/PIL)跑全链路;managed python(3.13.12)无 yaml,只能 py_compile 验证语法。PYTHONPATH=`apps/platform/src`。
